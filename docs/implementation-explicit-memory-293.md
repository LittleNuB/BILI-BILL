# 显式记忆实施合同

2026-09-10，#293，基线 main `275270b4d840982c41a2fc303e74efbdf148f8aa`。本文件随实现交付，不更改版本、发布或历史性能门禁。

- 只保存用户在表单确认的目标或偏好。普通提问、回答、会话摘要、Wiki 浏览都不建立记忆。聊天用户消息可作为表单草稿，必须再次确认，不能由模型宣称或调用工具自动保存。
- Dexie v16 新增独立本地表；旧数据不推断、不迁移为记忆。最多 32 项，每项正文 512 UTF-8 字节，记录总量 32 KiB；每次最多使用用户勾选的 6 项，受整体上下文预算限制。没有远程 Memory 服务或自动画像。
- 默认关闭独立记忆授权。首次开启明确说明选中目标/偏好可能发送到用户配置的 AI 服务；开启和浏览不发请求。保存与是否用于 AI 独立，勾选上限不能静默截断。
- 关闭、删除、编辑或取消勾选后新请求排除旧依赖讨论及其摘要，不删除本地聊天；进行中的请求中止。重新开启使用新授权代际，不恢复旧授权下的派生讨论。无法撤回之前已经发给服务的内容。
- 凭据形态和常见秘密标签在保存前拒绝；这是有限的本地检测，不能声称识别所有秘密。表单明确不要保存密码、密钥或登录信息。
- 修改、删除和清空使用修订校验；聊天关联记忆保存必须检查会话仍存在。删聊天默认保留独立记忆，确认框可选择同时删除关联记忆，在同一事务内删除；迟到表单不能重建记录。
- 学习资产/Wiki 和聊天备份不包含记忆，也不导入为记忆。首版不提供记忆导入/导出；管理页说明此限制。全部本地数据清理必须涵盖记忆及授权，保留修订防止迟到写入。
- 输入预算不足可少带或不带偏好，实际使用条数明确显示。偏好仅用于回答风格与目标，不作为视频事实、知识引用或更高优先级指令；不声称自动推断掌握程度。

真实模型是否合理应用偏好由用户验收；受控请求检查与合成 UI 通过不等于模型质量验收。
