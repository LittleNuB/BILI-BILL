# 扩展入口 UI 补齐 #282

2026-09-08。基线 main ea4d4cf。用户确认旧 popup 漏改并授权尽快补齐。

- 入口使用浅色紧凑布局，保留总览、动态账单、同步和统计。
- 诊断移至末尾并默认折叠；视频助手保留标题、分 P 和展开入口，无视频不显示无效知识节点。
- 进度环改为原生进度条，统计改为单行；修正展开区浅色文字对比度。
- 不改后台、AI 授权、存储、请求范围或跳转确认合同；不发布版本，不覆盖冻结面试包。

## 验证

- 685 Node tests 通过；typecheck、完整 build、release-dist 和 diff-check 通过。
- `tests/current-video-popup.mock-qa.py`：生产构建配合合成运行时，原有摘要、问答、会话隔离、迟到响应、预览/确认/返回及文案检查通过。该套件默认展开助手，以继续测试其原有行为。
- `tests/popup-native-ui.mock-qa.mjs`：生产构建配合合成运行时，380/320px 视频态、800px 无视频态、错误态、空数据态通过；检查折叠、导航、无自动生成/诊断请求、无横向溢出、无页面异常。
- 本地截图和构建绑定收据在 `release-artifacts/popup-282/`。以上不冒充真实 B 站验收，也不替代 #281。

## 本地加载

新候选独立放在 `release-artifacts/popup-282/extension/`。旧包不变。更换目录可能产生不同扩展身份，旧版只停用，不卸载；新目录不承诺自动迁移原扩展数据。没有读取个人浏览器文件、Cookie、登录态或凭据。
