# V0.14 功能候选进度

关联 #279。用户要求北京时间 2026-09-08 09:00–10:00 前争取完成整版可运行候选，允许耗时正式验收后置；不修改版本或发布标签，不改变 1000 条 / 10 MiB 及来源和数据保护合同。

## 已实现

- #274 新版视频浮层已合入 main `a173b30`；合并前 head `a0787a0` 三项 CI 成功。真实 B 站完整兼容性仍待 LG-5。
- LG-1 保存/书签基础已在 #278 合并。
- #279 工作分支：`codex/279-bounded-learning-candidate`，从当时最新 main 创建后接入 #274。
- 学习页搜索、类型/视频筛选、个人标题/备注/标签编辑及实际容量显示。
- 完整四类资产格式验证、规范备份、非破坏冲突副本及身份验证、独立清空 epoch；生产代码不导入实验脚本。
- 实际扩展 Worker 的导出、导入预览/确认、独立清空/确认及失败反馈。恢复提交重新读取最新本地数据做 CAS，不覆盖并发内容。
- LG-2 四类当前来源保存已接入：字幕原句、已验证摘要/亮点、整轮回答；后台重核来源，个人编辑不改原句。
- LG-3 搜索筛选、来源预览、明确确认后新标签打开、匹配播放器身份后定位与返回原位置。
- 全局浅色原生风格外壳及视频浮层已整合；设置/总览/收藏诊断说明按需展开，动态账单旧深色块已清理。
- Draft PR #280：<https://github.com/LittleNuB/BiliBili-DataViz-Plugin/pull/280>，main 已同步至 `a173b30`。

## 本轮验证

- 类型、构建及单 chunk 限制通过；新增 Worker 约 123 kB。
- 11 项相关存储/备份回归通过，包含与冻结 LG-0 编码/冲突算法对比、重复导入、个人编辑、容量拒绝、过期清空、取消和损坏文件。
- 工作树合成真实扩展运行 `release-artifacts/lg1/qa-4ce67e11-acec-4fcb-8106-2f23599f9e07`：12 组流程通过，包含原 LG-1 回归、桌面/窄屏、编辑/筛选、Worker 导出/幂等恢复/清空再恢复及完整资产对比。此报告早于纯合并函数输入隔离修正；修正后 Node 回归通过，最终候选会重建并复跑。
- 早期一轮测试因标题计数文案变化失败，随后保留原计数语义并加入容量，成功运行如上；不删除旧失败记录。
- 全量 Node 680/680 通过；此后补入字幕展示行号与原始片段的绑定映射回归，定向通过。早期合成源演练暴露展示 ID 不等于片段 ID，已用后台重建行绑定核对修复。
- `release-artifacts/lg1/source-ea51b0ba-a728-48d4-9bc9-a12e99676a93`：实际扩展中摘要/亮点/字幕/回答保存和读回、九页桌面/窄屏无横向溢出通过。固定模型响应仅演练流程，不是模型质量证据。
- `release-artifacts/lg1/qa-705e8522-9091-423c-83f7-ce6722592d22`：13 组流程通过，包含预览不跳转、确认定位、返回恢复原位置。新来源标签通过测试显式供应合成宿主页，不证明真实网络兼容。
- 整版启动器六类保存及完整浏览器重启读回通过；旧 LG-1 包和原启动器保留。
- Terra/xhigh 双轴关键复核完成：修复普通草稿跨视频归属、无界来源草稿、MV3 返回点易失问题。草稿按视频/分 P/类型/来源隔离，最多 16 份且 30 MiB 规范文本预算；超额拒绝新内容、不静默清除用户草稿。返回点仅在 `storage.session` 存 16 条最小会话票据，10 分钟过期，不保存原句。
- 修复回归 `qa-58867d6e-cab7-4025-b810-4eecaf3a3dd2`：14 项通过，包含实际停止拥有的扩展 service worker 后再返回，原位置仍恢复。
- Spec reviewer 经可达性核对撤回 local 来源误标 P0（当前生产 provider 仅 B站字幕）和重复点击 P1（同一动作随机 ID 重试幂等，关闭后明确新记录不按内容合并）。不为假设路径更改冻结合同；新增显式字幕 source 检查。
- 编辑及恢复期间加入站内导航保护；后台 Worker 中断后禁用旧会话操作并提示刷新，不在死 Worker 上继续等待。
- 最终代码相关全量回归 685/685 通过。整版启动器已跑通六类保存、完整浏览器重启、来源确认定位与返回；新标签由隔离演示预置空页再供应合成宿主页，不伪称真实网络验证。

## 候选交付

- 功能实现已齐备。最终同提交的真实扩展合成检查、冻结构建摘要和包路径写入 PR #280 与包内 verification.json；正式验收仍如下单列。
- `scripts/learning-candidate-demo.py` 为整版离线演示入口；`scripts/package-learning-candidate.mjs` 只接受同一干净提交、同构建摘要的两组 PASS 报告。旧 LG-1 演示脚本和包不改动。

## 后置验收

真实 B 站完整兼容性、生产生命周期中断场景及 LG-5 整版正式验收单列，不把合成页面或 Node PASS 宣称为真实站点/正式发布验收。旧 A2 和 #239/#240 不解锁；没有读取个人 Cookie/profile/密钥/登录态/数据库，没有自动 AI 请求。
