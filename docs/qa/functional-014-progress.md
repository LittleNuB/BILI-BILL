# V0.14 功能候选进度

关联 #279。用户要求北京时间 2026-09-08 09:00–10:00 前争取完成整版可运行候选，允许耗时正式验收后置；不修改版本或发布标签，不改变 1000 条 / 10 MiB 及来源和数据保护合同。

## 已实现

- #274 新版视频浮层已合入 main `a173b30`；合并前 head `a0787a0` 三项 CI 成功。真实 B 站完整兼容性仍待 LG-5。
- LG-1 保存/书签基础已在 #278 合并。
- #279 工作分支：`codex/279-bounded-learning-candidate`，从当时最新 main 创建后接入 #274。
- 学习页搜索、类型/视频筛选、个人标题/备注/标签编辑及实际容量显示。
- 完整四类资产格式验证、规范备份、非破坏冲突副本及身份验证、独立清空 epoch；生产代码不导入实验脚本。
- 实际扩展 Worker 的导出、导入预览/确认、独立清空/确认及失败反馈。恢复提交重新读取最新本地数据做 CAS，不覆盖并发内容。

## 本轮验证

- 类型、构建及单 chunk 限制通过；新增 Worker 约 123 kB。
- 11 项相关存储/备份回归通过，包含与冻结 LG-0 编码/冲突算法对比、重复导入、个人编辑、容量拒绝、过期清空、取消和损坏文件。
- 工作树合成真实扩展运行 `release-artifacts/lg1/qa-4ce67e11-acec-4fcb-8106-2f23599f9e07`：12 组流程通过，包含原 LG-1 回归、桌面/窄屏、编辑/筛选、Worker 导出/幂等恢复/清空再恢复及完整资产对比。此报告早于纯合并函数输入隔离修正；修正后 Node 回归通过，最终候选会重建并复跑。
- 早期一轮测试因标题计数文案变化失败，随后保留原计数语义并加入容量，成功运行如上；不删除旧失败记录。

## 继续实现

- LG-2 当前可靠字幕摘录、已验证摘要/亮点/答案保存，来源绑定及过期保护。
- LG-3 来源预览、确认跳转、返回。
- 统一总览与学习页外壳视觉，去掉冗余文字/装饰，保留既有导航和操作。
- 完整候选打包、操作说明与最终关键回归，保留旧 LG-1 演示包兜底。

## 后置验收

真实 B 站完整兼容性、生产生命周期中断场景及 LG-5 整版正式验收单列，不把合成页面或 Node PASS 宣称为真实站点/正式发布验收。旧 A2 和 #239/#240 不解锁；没有读取个人 Cookie/profile/密钥/登录态/数据库，没有自动 AI 请求。
