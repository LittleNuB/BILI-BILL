# 视频 Wiki 验证记录

2026-09-10，关联 #292。基线 main `4f44e4b394c6ef3b523126945b1bb15de0c21f4c`。技术边界见 [实施合同](implementation-video-wiki-292.md)。这是候选功能证据，不是正式发布或用户真实资料验收。

## 已实施

- 一个 BV 一个 Wiki 页，页内真实分 P；现有摘要/要点、笔记、摘录和已保存回答按区展示，共用原资产及编辑并发保护。
- 首次成功新增资产建页，删页默认保留资产；单独勾选再确认才同时删除该视频内容。墓碑及库代际阻止旧保存复活，新明确保存可重建，不恢复旧手动归属。
- 本地词项聚合与有限同义表达，主题可重叠；手动主题、新名称、加入/排除持久化并优先。没有正文不依标题编造分类结论，不运行模型或远程向量请求。
- 来源预览、确认定位、返回复用原来源路径。笔记编辑用原生模态窗口，导航保护及失败保留内容；只读刷新不阻止切页。归属工具默认收起，桌面/窄屏布局检查通过。
- Markdown 只导出原资产；联合备份含组织/删除记录/手动决定，旧备份可导入。恢复预览明确资产合并与组织替换，写入原子化。空视频页可合法备份。清空学习笔记同时清空 Wiki，拒绝过期组织预览。
- Wiki 写入没有新模型正文类型、额外全字幕存储或后台 AI。学习请求/来源模块改为动态加载，使后台 JS 回到约 488.19 kB，未放宽 500000 字节上限。

## 验证结果

- `npm test`：773/773。首次全量中两个测试仍断言旧数据库 v14；更新为 v15/24 表并保留所有旧数据与旧表 schema 比较后通过。
- `npm run build`：包含类型检查及发布产物结构/许可证/块大小校验，通过。
- `git diff --check`：通过。
- 新增 Wiki repository/topics/backup 测试 24 条，覆盖迁移、两视频/多 P、新保存和幂等、迟到提交、删页、清空预览、单份编辑、容量回滚、人工主题、重新打开、新旧备份及取消。
- `tests/video-wiki.mock-qa.mjs`：真实生产 Wiki/备份 worker + 隔离 Chrome + 合成 IndexedDB，通过就地编辑、主题新建/加入/重命名、来源预览/确认/返回、三种窗口布局、Markdown、删页后重载、联合备份导出及确认恢复。笔记编辑和来源消息桥为受控 stub；仓储行为由真实 repository 测试另行覆盖。
- 原 `tests/knowledge-chat.mock-qa.mjs`、`tests/unified-composer.mock-qa.mjs`：通过，知识引用与快速记笔记未见回归。
- 截图与合成报告位于忽略目录 `release-artifacts/wiki-292/`，不包含真实用户数据。

## 复核与限制

本轮两名未参与实现的 5.6 Terra / xhigh 独立复核发现一个 P1（旧备份预览后修改未阻止恢复）和三个不同 P2（自动主题配额释放、空页导出、主题名称草稿导航保护）。均已定点修复并补回归：旧备份资产/组织修订在提交事务内复查；显式手动名称标记；空页禁用导出；名称输入草稿阻止离开。两位原 reviewer 均已定点确认全部问题关闭，没有开启新一轮全面复核。修复后 focused 32/32、全量 773/773、生产 Wiki UI 回归和构建通过。

首轮 GitHub validate 在 Playwright 系统依赖安装阶段遇到 Google apt 源 Hash Sum mismatch，未运行项目验证；其 npm ci audit 为 0 vulnerabilities。随修复提交重新执行 CI，不禁用校验和或放宽项目阈值。

真实库迁移、真实 B 站定位与真实模型检索效果仍由整版验收确认。数据表升级至 v15 后，旧 v14 构建不能作为无条件数据库回滚方案；加载候选前应保留学习备份，不通过删库解决版本兼容。

没有读取 Cookie、浏览器 profile、登录态或密钥文件；没有调用真实模型、发布版本、修改 package/manifest 版本或 tag。显式长期记忆 #293 尚不在本 PR 内。
