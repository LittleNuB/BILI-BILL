# LG-0 验收与生效记录

日期：2026-09-07。关联 #270 / #271。主 Agent / PM 裁决：**接受有限合同 v1 与隔离验证原型；PR #271 审查合并后 LG-0 完成，LG-1 #277 可开始。** PR 未合并时仍不启动生产实现。本记录不修改任何原始 verdict，也不是 V0.14.0 发布申请。

## 证据分层

1. 正式测量提交 `8f273d77dbfc17759fa5ad7b632e7ce6a8757e97`：六场景 48 次 Worker 性能矩阵和独立安全收据通过；提交 blob、源码与实际 bundle 在运行前绑定并由独立 verifier 复建验证。最大合计堆采样增长 161.271 MiB，保持 256 MiB 预算。报告与方法详见 [证据入口](../benchmarks/lg0/README.md)。
2. 最终代码提交 `4c804cae90eedc4d11117151982ccecb23c54a13`：中立复核指出的稀疏数组漏洞由标签逐项验证、按索引比较修复。两项回归先红后绿，覆盖 merge 输入拒绝和 change 不漏写旧稀疏行的密集修复。42/42 相关、662/662 全量、typecheck/build/diff-check 通过；该干净提交九次同工作流短诊断全部通过，最大增长 180.092 MiB。
3. 不混绑：第 1 层的 48 次性能与安全结果只证明 8f273d7；最终代码没有完整重跑，不把旧 bundle 哈希当作最终 bundle，也不把九次诊断当 p95。主 Agent 按用户确认的风险匹配测试节奏接受这个输入边界补丁：它不改变合法容量、备份字节或正常稠密内容语义，不重新引入全量比较字符串；针对性回归与同工作流诊断覆盖其风险。生产抽取与接入仍须 LG-1 至 LG-5 的实际验收。
4. 原始单项 verifier 固定 lg1Unlocked=false，表示收据自身不能决定开发准入。本记录结合用户职责裁决、合同/代码审查、回归、合并状态决定 LG-0 准入；不通过改写单项输出伪装自动放行。

## 验收映射

| 要求 | 本次依据与结论 |
| --- | --- |
| 字段/稳定身份/不可变来源/可编辑个人层 | 合同 v1 与 schema/来源/身份回归通过；生产捕获适配归 LG-1/LG-2 |
| 1000 项 / 10 MiB / 超 1 拒绝 | 规范字节、引用与导入身份全部计数；六合法场景及数量/字节超限测试通过 |
| 并发/清空/迟到写入 | CAS 重试从最新状态重算；epoch 与并发创建/编辑/恢复/删除回归通过 |
| 旧库与持久性 | 真实 v13 类创建的 21 个合成旧表升级保留、失败回滚；18 次冷重开核对完整摘要 |
| 搜索 | 已保存字段 AND 子串、NFKC/小写、类型/视频筛选；删除/导入一致性通过；cold p95 最大 219.4ms、warm 最大 209ms，未使用全局平均掩盖场景 |
| 恢复完整性 | 唯一规范 JSON、UTF-8/白名单/身份、文件上限、冲突副本、重复导入、最终容量与零部分写入回归通过 |
| 取消/事务故障 | 10 MiB 导入/导出取消确认 75.3/46.3ms，至少 2s 后完整状态不变；真实 renderer bulkPut 后中断、类型化浏览器 quota 拒绝及恢复重开通过 |
| 内存/响应 | 正式合法矩阵无观测 >=50ms 主线程任务；2s 可取消进度、250ms 样本质量和256MiB增长预算通过；三类敌意输入拒绝与采样通过，非绝对峰值/RSS保证 |
| 旧 B1 / 构建 / 隐私 | 8f273d7 的 CI 34131820293 三项成功，含独立历史 B1 重建验证；本地旧产物哈希与祖先核对不变；无生产/manifest/package变更，无私人数据读取或网络采集 |
| 中立审查 | Terra/xhigh 无 P0/P1；稀疏数组 P2 已修复，证据分层 P2 在本记录与 README 首段明确处理。后续只改归档/冻结文档，不更改已验证运行时 |

## 后续必需项

用户已确认 LG-0 只验隔离原型，消除禁止生产接入却要求先验生产保护的循环依赖。生产升级/保存重开归 LG-1，原文与答案保存/证据适配归 LG-2，检索回看与预览确认返回归 LG-3，生产备份恢复/容量/重置/扩展生命周期故障归 LG-4，LG-5 复验整体并依赖 UX-014。

旧 A2 仍未通过，旧 #239/#240 不启动；旧 B1 和所有 LG-0 历史失败原样保留。不得以此关闭旧完整知识库任务、改变发布版本或发布产品。下一项 #277 仅实现笔记/书签保存、关闭重开及最小列表，不提前加入摘录、跨视频 AI 或整库能力。
