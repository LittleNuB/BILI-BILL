# 显式记忆验证记录

2026-09-10，#293。基线 main `275270b4d840982c41a2fc303e74efbdf148f8aa`。实施范围与有限容量见 [实施合同](implementation-explicit-memory-293.md)。

## 已实现

- 设置内目标/偏好列表、确认保存、选用、编辑、删除、清空；独立默认关闭 AI 授权，开启前说明。
- 聊天用户消息的书签图标只打开可编辑草稿，确认后保存。删聊天默认保留独立记忆，可勾选同时删关联记忆。
- v16 新增表，不推断已有资料。修订检查、会话存在检查、关联删除事务、全部本地数据清理接入。
- 记忆进入现有上下文预算；记录依赖代际，撤销/编辑/删除后排除派生讨论及摘要，运行中的请求取消，历史仍在。
- 常见凭据形态拒绝，普通对话不自动记住。记忆明确不包含在现有学习/Wiki/聊天备份内，首版无记忆备份。

## 验证

- focused `explicit-memory`、`knowledge-chat`、`learning-chat`：47/47，其中新增记忆 16 条。
- 类型检查与生产构建通过；真实后台集成修复后后台约 424.95 kB，Dexie 独立静态块约 95.25 kB，未放宽单块 500000 字节上限。
- `tests/explicit-memory.mock-qa.mjs`：生产 UI + 受控消息桥 + Node fake IndexedDB 中的真实记忆 repository，通过设置增改删、过期修订保留草稿、敏感内容拒绝、草稿导航保护、三种布局、开启不自动调用模型、聊天二次确认与删除参数。
- 合成截图及报告：忽略目录 `release-artifacts/memory-293/`。模型请求/取消由生产入口测试另行覆盖；浏览器脚本不等同于完整真实扩展后台和 B 站验收。
- 全量 `npm test`：792/792。原类别注册测试补入显式记忆，原数据库升级测试继续保留全部旧表/数据校验，并断言新记忆表为空；含三条后台静态模块图回归。
- 两名未参与实现的 Terra / xhigh 关键中立复核：规格轴无 P0/P1/P2；工程轴无 P0/P1，一条 P2 为测试报告仍写“dynamic imports”。已改成静态模块图的准确描述，最终收据在干净提交重跑，旧收据不冒充最终证据。

## 真实扩展增补

`tests/explicit-memory.extension-qa.mjs` 使用全新隔离 Chromium、生产扩展、真实消息分发与浏览器 IndexedDB，合成偏好不含账号或模型配置。首次发现 Vite 动态导入在 MV3 下报错；仅关闭预载仍失败，最终改静态模块图后保存、整浏览器关闭/重开精确读回、删除与迟到编辑拒绝均通过，学习库/关闭态聊天入口、Wiki worker 与 popup 可打开。另加三条构建依赖图测试，阻止后台动态导入回归。

临时合成浏览器已全部关闭；清理目录命令被当前工具策略拒绝，保留在忽略的 `release-artifacts/memory-293/runtime-*/synthetic-browser` 下，不读取其内容、不打入候选包。报告 `profileRemoved=false`，不宣称清理通过。最终可复用收据须绑定干净提交和完整 dist 文件哈希。

未读取私人数据库、Cookie、profile、登录态或密钥文件；未调用真实模型。真实偏好应用效果、用户资料升级和跨标签页体验仍待整版验收。没有版本/tag/release 变更。
